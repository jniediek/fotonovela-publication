# Fotonovela figure scripts

These scripts produce the figures in the publication: ...

The plots are produced by Python scripts using matplotlib, one per figure in `scripts/`.

This project is maintained with `uv`. To reproduce a figure from the manuscript, clone the repository, and run `uv sync` in the repository root.
